import { CasefromatPipe } from './casefromat.pipe';

describe('CasefromatPipe', () => {
  it('create an instance', () => {
    const pipe = new CasefromatPipe();
    expect(pipe).toBeTruthy();
  });
});
